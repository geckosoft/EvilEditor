#include Traps_IndoorFriends.pat

// IsIndoorTileSet means this object can be placed indoors
IsIndoorTileSet

// IsOutdoorTileSet means this object can be placed outdoors
IsOutdoorTileSet

// DoNotPress Trap
OverlayPattern=1,1 for 17207
F/*	
EndList